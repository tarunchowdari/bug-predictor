# ============================================================
# forge-bug-predictor — requirements.txt
# All packages pinned to a compatible major version.
# Install with: pip install -r requirements.txt
# ============================================================

# ── Data / IO ────────────────────────────────────────────────
pandas>=2.1,<3.0
pyarrow>=15.0,<16.0          # parquet read/write backend
numpy>=1.26,<2.0

# ── Mining ───────────────────────────────────────────────────
pydriller>=2.5,<3.0
gitpython>=3.1,<4.0

# ── NLP / Embeddings ─────────────────────────────────────────
sentence-transformers>=2.7,<3.0
transformers>=4.40,<5.0
tokenizers>=0.19,<1.0

# ── AST Parsing ──────────────────────────────────────────────
tree-sitter>=0.21,<1.0
tree-sitter-python>=0.21,<1.0
tree-sitter-javascript>=0.21,<1.0

# ── ML / XGBoost ─────────────────────────────────────────────
xgboost>=2.0,<3.0
scikit-learn>=1.4,<2.0
imbalanced-learn>=0.12,<1.0
optuna>=3.6,<4.0
shap>=0.45,<1.0
matplotlib>=3.8,<4.0
joblib>=1.3,<2.0

# ── Deep Learning ─────────────────────────────────────────────
torch>=2.2,<3.0
torchvision>=0.17,<1.0       # optional, pulled in by some torch utilities

# ── MLflow ───────────────────────────────────────────────────
mlflow>=2.12,<3.0

# ── API ───────────────────────────────────────────────────────
fastapi>=0.111,<1.0
uvicorn[standard]>=0.29,<1.0
pydantic>=2.7,<3.0
httpx>=0.27,<1.0             # async HTTP for webhook fan-out
python-multipart>=0.0.9,<1.0

# ── Utilities ─────────────────────────────────────────────────
typer>=0.12,<1.0
rich>=13.7,<14.0             # pretty CLI output
tqdm>=4.66,<5.0
python-dotenv>=1.0,<2.0
requests>=2.31,<3.0
